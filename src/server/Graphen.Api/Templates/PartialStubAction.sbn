using System;
using System.Threading;
using System.Threading.Tasks;
using Microsoft.AspNetCore.Mvc;
using {{ project_name }}.DTOs;

namespace {{ project_name }}.Controllers;

public sealed partial class {{ controller_name }}Controller
{
    public partial async Task<IActionResult> {{ name }}({{ request_type }} request, CancellationToken ct)
    {
        // TODO: Реализуйте метод контроллера
        throw new NotImplementedException();
    }
}
